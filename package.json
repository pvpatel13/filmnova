{
  "name": "filmnova-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "@vercel/analytics": "^2.0.1"
  }
}